{{/*
Copyright (c) 2022 Nitro Agility S.r.l.
SPDX-License-Identifier: Apache-2.0
*/}}
{{/*
Two services per plane, and that is the point rather than an accident.

The public service is what plane callers reach. The service whose stable chart name is `telemetry`
exposes the Server Host operations surface to Prometheus and the kubelet. It is separate so that a
NetworkPolicy, an ingress or a mesh can allow one without allowing the other: process discovery and
metrics describe the inside of the process, and health tells an attacker when it is struggling.
*/}}
{{- range $component, $plane := dict "control-plane" .Values.controlPlane "data-plane" .Values.dataPlane }}
{{- if $plane.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "permguard.fullname" $ }}-{{ $component }}
  labels:
    {{- include "permguard.componentLabels" (dict "root" $ "component" $component) | nindent 4 }}
  {{- with $plane.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ $plane.service.type }}
  selector:
    {{- include "permguard.selectorLabels" (dict "root" $ "component" $component) | nindent 4 }}
  ports:
    - name: http
      port: {{ $plane.ports.http }}
      targetPort: http
      protocol: TCP
    {{- if ne (int $plane.ports.grpc) (int $plane.ports.http) }}
    - name: grpc
      port: {{ $plane.ports.grpc }}
      targetPort: grpc
      protocol: TCP
    {{- end }}
---
{{- if include "permguard.perReplica" (dict "root" $ "plane" $plane) }}
{{/*
The Service a StatefulSet is named after: no cluster IP, so it resolves to the pods themselves.

The ordinary Service above load balances requests. This one publishes the stable per-pod DNS names
Kubernetes requires for the StatefulSet and lets an operator address the replica that owns a
particular persistent journal. It does not imply that independently owned temporal histories are
request-time coherent; the chart refuses that multi-replica topology separately.

`publishNotReadyAddresses`, because a pod's name has to resolve while it is still coming up —
recovering a journal, replaying a history — and a name that appears only once the pod is ready is
not an address, it is a race.
*/}}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "permguard.fullname" $ }}-{{ $component }}-headless
  labels:
    {{- include "permguard.componentLabels" (dict "root" $ "component" $component) | nindent 4 }}
spec:
  type: ClusterIP
  clusterIP: None
  publishNotReadyAddresses: true
  selector:
    {{- include "permguard.selectorLabels" (dict "root" $ "component" $component) | nindent 4 }}
  ports:
    - name: http
      port: {{ $plane.ports.http }}
      targetPort: http
      protocol: TCP
    {{- if ne (int $plane.ports.grpc) (int $plane.ports.http) }}
    - name: grpc
      port: {{ $plane.ports.grpc }}
      targetPort: grpc
      protocol: TCP
    {{- end }}
---
{{- end }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "permguard.fullname" $ }}-{{ $component }}-telemetry
  labels:
    {{- include "permguard.componentLabels" (dict "root" $ "component" $component) | nindent 4 }}
  annotations:
    {{- include "permguard.metricsAnnotations" (dict "root" $ "telemetryPort" $plane.ports.telemetry) | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    {{- include "permguard.selectorLabels" (dict "root" $ "component" $component) | nindent 4 }}
  ports:
    - name: telemetry
      port: {{ $plane.ports.telemetry }}
      targetPort: telemetry
      protocol: TCP
---
{{- end }}
{{- end }}
{{- if .Values.allInOne.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "permguard.fullname" . }}-all-in-one
  labels:
    {{- include "permguard.componentLabels" (dict "root" . "component" "all-in-one") | nindent 4 }}
  {{- with .Values.allInOne.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.allInOne.service.type }}
  selector:
    {{- include "permguard.selectorLabels" (dict "root" . "component" "all-in-one") | nindent 4 }}
  ports:
    - name: control-http
      port: {{ .Values.allInOne.ports.controlHttp }}
      targetPort: control-http
    - name: data-http
      port: {{ .Values.allInOne.ports.dataHttp }}
      targetPort: data-http
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "permguard.fullname" . }}-all-in-one-telemetry
  labels:
    {{- include "permguard.componentLabels" (dict "root" . "component" "all-in-one") | nindent 4 }}
  annotations:
    {{- include "permguard.metricsAnnotations" (dict "root" . "telemetryPort" .Values.allInOne.ports.telemetry) | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    {{- include "permguard.selectorLabels" (dict "root" . "component" "all-in-one") | nindent 4 }}
  ports:
    - name: telemetry
      port: {{ .Values.allInOne.ports.telemetry }}
      targetPort: telemetry
{{- end }}
